services:
  - type: web
    name: nifty100-analytics
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn nifty100_project.wsgi:application --bind 0.0.0.0:\
